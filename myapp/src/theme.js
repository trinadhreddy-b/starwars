import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#e53935', // Replace with your desired header background color
    },
    text: {
      primary: '#ffffff', // Replace with your desired header text color
    },
  },
});

export default theme;
